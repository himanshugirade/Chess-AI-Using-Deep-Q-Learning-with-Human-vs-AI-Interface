import chess
import random

board = chess.Board()

while not board.is_game_over():
    print(board)
    move = random.choice(list(board.legal_moves))
    board.push(move)

print("Game Over:", board.result())